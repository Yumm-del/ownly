services:
  - type: web
    name: ownly
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: python app.py
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.7
